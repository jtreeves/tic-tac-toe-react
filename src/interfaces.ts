interface PointsProp {
    points: number[]
}

interface IdentityProp {
    id: string
}

interface PlayerProp {
    player: number
}

interface MessageProp {
    message: string
}

export type {
    PointsProp,
    IdentityProp,
    PlayerProp,
    MessageProp
}